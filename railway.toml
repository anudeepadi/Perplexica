[build]
builder = "nixpacks"

[deploy]
startCommand = "cd .next/standalone && PORT=${PORT:-3000} node server.js"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

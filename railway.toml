[build]
builder = "nixpacks"

[build.nixpacks]
# Next.js needs these for the build
installCmds = ["npm install"]
buildCmds = ["npm run build"]

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
